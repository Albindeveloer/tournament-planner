{
  "name": "@tournament-planner/api-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "API Gateway — single public entry point for routing, authentication boundary, and rate limiting",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test-watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "eslint": "^10.10.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "@fastify/http-proxy": "^11.6.2",
    "@fastify/jwt": "^10.2.2",
    "dotenv": "^17.4.2",
    "fastify": "^5.12.4"
  }
}
